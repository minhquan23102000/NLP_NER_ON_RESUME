const { render } = require('./index');
const { JSDOM } = require('jsdom');

module.exports = {
  createDocument: function(resume) {
    const { window } = new JSDOM(render(resume || {}));
    const { document } = window;
    return document;
  }
};
